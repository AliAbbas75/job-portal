/** Which staff roles can do what. The backend enforces the same rules. */
export const CREATOR_ROLES = ['job_creator', 'admin'];
export const APPROVER_ROLES = ['approver', 'admin'];
export const PUBLISHER_ROLES = ['admin'];
