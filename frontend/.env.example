# Serve API calls from in-browser mock data (true) or the Flask backend (false).
# Keep true until the backend endpoints for a page exist. Jobs and reference data are ready (M4);
# profile and documents need M2's login first. Override locally in .env.local (gitignored).
VITE_USE_MOCKS=true
